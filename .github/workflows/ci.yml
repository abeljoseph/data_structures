name: CI

on: [push, watch]


jobs:
  test:

    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Setup Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8.3'
    - name: Install
      run: |
        echo "Installing"
    - name: Test
      run: |
        echo "Testing"
